name: Deploy Site (App + Docs) to GitHub Pages [DEPRECATED]

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-deploy
  cancel-in-progress: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use deploy.yml. This workflow is deprecated."


